# Тяжёлый стек для inference в Streamlit Cloud (1 vCPU, CPU-only torch).
#
# index-url замещает default-pypi на CPU-сборку PyTorch — без этого pip может
# забрать CUDA-сборку (~750 МБ) и приложение не влезет в 1 ГБ диска.
# extra-index-url возвращает доступ к pypi для прочих пакетов (streamlit и т.д.).
#
# Версии torch специально не фиксируются: Streamlit Community Cloud берёт
# свежий Python (3.13–3.14), и для него доступны только torch 2.9+. Pip сам
# выберет совместимый CPU-wheel.

--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

torch
torchvision

streamlit>=1.42
numpy
pandas
opencv-python-headless
pillow
requests
ultralytics
rfdetr
