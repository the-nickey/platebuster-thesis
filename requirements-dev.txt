# Development & training requirements
# Inherits from requirements.txt + adds training, notebooks, eval, dataset prep

-r requirements.txt

# Additional model architectures (used in comparative experiment)
rfdetr>=1.0.0

# Training & eval
scikit-learn>=1.3.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Notebook environment
jupyter>=1.0.0
jupyterlab>=4.0.0
ipykernel>=6.25.0
ipywidgets>=8.1.0

# Dataset utilities
gdown>=5.0.0           # download from Google Drive (CCPD)
roboflow>=1.1.0        # pull Roboflow Universe datasets via API
tqdm>=4.65.0
pillow-heif>=0.13.0    # iPhone HEIC photos support (own dataset)

# CV-extended
opencv-contrib-python-headless>=4.8.0  # extra algorithms for classical baseline

# Code quality
ruff>=0.1.0
