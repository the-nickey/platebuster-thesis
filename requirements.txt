# Production requirements (Streamlit Cloud deploy)
# Slim — only what's needed to run the inference web app.
# For training/jupyter dependencies see requirements-dev.txt

# Core ML inference
ultralytics>=8.4.0
torch>=2.4.0
torchvision>=0.19.0

# Image processing
opencv-python-headless>=4.8.0
pillow>=10.0.0
numpy<2.0

# Web app
streamlit>=1.30.0

# Utils
pyyaml>=6.0
